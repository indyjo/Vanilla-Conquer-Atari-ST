profile on
# Do not use :noinit here. Instruction counts are only finalized in
# DebugCpu_InitSession() -> Profile_CpuStop(), which :noinit skips.
# Callers are updated live, so save still writes the graph with 0 addrs.
b pc = HatariProfileEnd :once :trace :file profile-off.ini
